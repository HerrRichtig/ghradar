# GitHub 个人访问令牌（可选但强烈建议）。未配置时 Search API 限速 10 次/分钟，
# 配置后 30 次/分钟，完整抓取快 3 倍。只读权限即可：勾选 public_repo（或无需任何 scope）。
# 生成地址：https://github.com/settings/tokens
GITHUB_TOKEN=

# 索引数据目录（默认 ./data）
# GHRADAR_DATA_DIR=./data

# 向量模型（默认多语言 MiniLM，384 维，支持中英跨语言检索）
# GHRADAR_EMBED_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
